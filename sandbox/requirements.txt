# Libraries available to scripts and notebooks in the sandbox. The sandbox filesystem is
# read-only, so scripts can't pip install: add what you need here, then rebuild the image:
#   sudo docker build -t remotepy-sandbox:latest sandbox/
numpy
pandas
scipy
matplotlib
seaborn
sympy
pillow
scikit-learn
tabulate
requests
beautifulsoup4
# Jupyter: runs uploaded .ipynb notebooks, and notebook-style imports (IPython.display) work in scripts
ipython
ipykernel
nbclient
nbformat
# CPU-only PyTorch adds ~1 GB to the image; uncomment if you want it:
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch
